# dotfiles

個人的な設定ファイル管理用リポジトリ

## 環境
- macOS
- Windows (予定)

## 管理しているファイル
- `.zshrc` - zsh設定
- `.gitconfig` - Git設定
- `starship.toml` - Starshipプロンプト設定

## セットアップ
```bash
git clone git@github.com:mishi5/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```
