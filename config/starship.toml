# Starship設定ファイル
# ドキュメント: https://starship.rs/config/

# プロンプトのフォーマット（表示順序）
format = """
$username\
$hostname\
$directory\
$git_branch\
$git_status\
$nodejs\
$python\
$rust\
$golang\
$docker_context\
$time\
$line_break\
$character"""

# ディレクトリ表示
[directory]
truncation_length = 3       # 最大3階層まで表示
truncate_to_repo = true     # Gitリポジトリのルートまで表示
style = "bold cyan"
format = "[$path]($style)[$read_only]($read_only_style) "

# Gitブランチ
[git_branch]
symbol = " "
format = "on [$symbol$branch]($style) "
style = "bold purple"

# Git状態
[git_status]
format = '([$all_status$ahead_behind]($style) )'
style = "bold red"
conflicted = "🏳"
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
untracked = "?${count}"
stashed = "$${count}"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "✘${count}"

# 時刻表示
[time]
disabled = false
format = '[$time]($style) '
time_format = "%m/%d %H:%M:%S"  # 日付+時刻にしたい場合
style = "bold blue"
use_12hr = false

# Node.js
[nodejs]
format = "via [$symbol($version )]($style)"
symbol = " "
style = "bold green"
detect_files = ["package.json", ".node-version"]
detect_folders = ["node_modules"]

# Python
[python]
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
symbol = " "
style = "bold yellow"

# Rust
[rust]
format = "via [$symbol($version )]($style)"
symbol = " "
style = "bold red"

# Go
[golang]
format = "via [$symbol($version )]($style)"
symbol = " "
style = "bold cyan"

# Docker
[docker_context]
format = "via [$symbol$context]($style) "
symbol = " "
style = "bold blue"
only_with_files = true

# コマンド入力記号
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold green)"

# ユーザー名（デフォルトで非表示、SSH時のみ表示）
[username]
style_user = "bold yellow"
style_root = "bold red"
format = "[$user]($style) "
disabled = false
show_always = false  # 常に表示したい場合はtrue

# ホスト名（SSH時のみ表示）
[hostname]
ssh_only = true
format = "on [$hostname]($style) "
style = "bold green"
disabled = false
